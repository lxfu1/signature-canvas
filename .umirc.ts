import { defineConfig } from 'dumi';

export default defineConfig({
  title: 'signature-canvas',
  base: '/signature-canvas/',
  publicPath: '/signature-canvas/',
  // more config: https://d.umijs.org/config
});
